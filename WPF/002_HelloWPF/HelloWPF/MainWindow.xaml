<!--
xaml 语言
    - 是一种声明性语言
        - 每见到一个标签，xaml编译器会为我们声明一个 该标签所对应的对象
-->
    
<!--
    xmlns 是引入名称空间，引入一系列名称空间
        - ns， 就是 namespace
        - :x， 是给命名空间起的名字，第一个没有起名字，是默认名称空间
    
    
    xaml 文件中的 MainWindow类 编译后生成的类，和 cs文件中的 MainWindow类 编译后生成的类是同一个类。
--> 
<Window x:Class="HelloWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Hello WPF!" Height="200" Width="300" WindowStyle="ToolWindow">
    <Grid>

    </Grid>
</Window>
